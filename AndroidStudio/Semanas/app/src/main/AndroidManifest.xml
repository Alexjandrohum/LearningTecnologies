<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alexjandrohum.semanas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Horario AppTec"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PerfilAlumno"
            android:label="@string/title_activity_perfil_alumno"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Semana" />
        <activity android:name=".Dia" />
        <activity android:name=".Perfil" />
        <activity
            android:name=".MainActivity"
            android:label="Horario Día"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainActivity2"
            android:label="Horario Semana"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Acerca"
            android:label="@string/title_activity_acerca"
            android:theme="@style/AppTheme" />
    </application>

</manifest>